{
  "name": "sporty-take-home-api-automation",
  "version": "1.0.0",
  "description": "Assignment B API automation tests using JavaScript and Jest.",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand && node scripts/normalizeReportStatus.js",
    "test:report": "jest --runInBand && node scripts/normalizeReportStatus.js",
    "test:verbose": "jest --runInBand --verbose && node scripts/normalizeReportStatus.js"
  },
  "keywords": [
    "api-testing",
    "jest",
    "automation"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "dotenv": "^17.4.2",
    "jest": "^29.7.0",
    "jest-html-reporters": "^3.1.7"
  }
}
